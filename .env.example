# HTTP Proxy Configuration
# 用于需要翻墙的库 | For libraries that require proxy
HTTP_PROXY=http://127.0.0.1:8118
HTTPS_PROXY=http://127.0.0.1:8118

# 如果需要排除某些域名不走代理，可以设置 NO_PROXY
# NO_PROXY=localhost,127.0.0.1

# LibreOffice Configuration
# LibreOffice 安装路径 | LibreOffice installation path
# macOS 默认路径 | macOS default path
LIBREOFFICE_PATH=/Applications/LibreOffice.app/Contents/MacOS/soffice
# Linux 默认路径 | Linux default path
# LIBREOFFICE_PATH=/usr/bin/soffice
# Windows 默认路径 | Windows default path
# LIBREOFFICE_PATH=C:\Program Files\LibreOffice\program\soffice.exe

# UNO Python 路径 | UNO Python path (if needed)
# PYTHONPATH=/Applications/LibreOffice.app/Contents/Resources/
